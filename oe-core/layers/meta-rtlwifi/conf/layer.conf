# Append conf dir to BBPATH
BBPATH .= ":${LAYERDIR}"

# Append recipe dir to BBFILES
BBFILES += "${LAYERDIR}/recipes*/*/*.bb ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-rtlwifi"
BBFILE_PATTERN_meta-rtlwifi = "${LAYERDIR}/"
BBFILE_PRIORITY_rtlwifi = "1"
